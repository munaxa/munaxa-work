/**
 * Shared test builders, object mothers, fixtures, fake repositories, a test event dispatcher and shared assertions. Filled in Phase 1, once the kernel abstractions they build on exist.
 *
 * Bootstrapped here so that the package, its build and its place in the project references
 * graph are proven before anything depends on them. It deliberately exports nothing yet.
 */
export {};
